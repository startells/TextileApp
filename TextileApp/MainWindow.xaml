<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="TextileApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TextileApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar x:Name="TitleBar"
              Title="Система управления"
              IsPaneToggleButtonVisible="True"
              PaneToggleRequested="TitleBar_PaneToggleRequested"
              IsBackButtonVisible="{x:Bind ContentFrame.CanGoBack, Mode=OneWay}"
              BackRequested="TitleBar_BackRequested" />

        <NavigationView x:Name="NavView"
                    Grid.Row="1"
                    IsBackButtonVisible="Collapsed"
                    IsPaneToggleButtonVisible="False"
                    DisplayModeChanged="NavView_DisplayModeChanged"
                    IsSettingsVisible="False">

            <NavigationView.MenuItems>

                <NavigationViewItemHeader Content="Главная"/>
                
                <NavigationViewItem Content="Панель управления" Icon="Home"/>
                <NavigationViewItem Content="Заказы" Icon="Bullets"/>
                <NavigationViewItem Content="Сотрудники" Icon="Contact"/>
                
                <NavigationViewItemHeader Content="Склад"/>
                <NavigationViewItem Content="Продукция" Icon="Shop"/>
                <NavigationViewItem Content="Материалы" Icon="Placeholder"/>
                <NavigationViewItem Content="Фурнитура" Icon="MapPin"/>

            </NavigationView.MenuItems>

            <Frame x:Name="ContentFrame"/>
            
        </NavigationView>
    </Grid>

</Window>
