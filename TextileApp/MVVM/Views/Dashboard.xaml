<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TextileApp.MVVM.Views.Dashboard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TextileApp.MVVM.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="20" RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding ViewModel.Title}" 
                   FontSize="32" 
                   FontWeight="Bold"
                   Foreground="{ThemeResource TextFillColorPrimary}"/>

        <Grid Grid.Row="1" Padding="20" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Материалы" FontSize="16" Foreground="{ThemeResource TextFillColorSecondary}"/>
                <TextBlock Text="{Binding ViewModel.MaterialsCount}" FontSize="48" FontWeight="Bold"/>
            </StackPanel>

            <Button Grid.Column="1" 
                    VerticalAlignment="Center"
                    Command="{Binding ViewModel.LoadDataCommand}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <SymbolIcon Symbol="Refresh"/>
                    <TextBlock Text="Обновить"/>
                </StackPanel>
            </Button>
        </Grid>

        <Grid Grid.Row="2" Padding="20" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Активные заказы" FontSize="16" Foreground="{ThemeResource TextFillColorSecondary}"/>
                <TextBlock Text="{Binding ViewModel.ActiveOrdersCount}" FontSize="48" FontWeight="Bold"/>
            </StackPanel>

            <Button Grid.Column="1" 
                    VerticalAlignment="Center"
                    Command="{Binding ViewModel.LoadDataCommand}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <SymbolIcon Symbol="Refresh"/>
                    <TextBlock Text="Обновить"/>
                </StackPanel>
            </Button>
        </Grid>

    </Grid>
</Page>
